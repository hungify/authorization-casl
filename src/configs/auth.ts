export enum ROLES {
  Admin = 'Admin',
  Editor = 'Editor',
  Subscriber = 'Subscriber',
  Guest = 'Guest',
}

export enum ACTIONS {
  create = 'create',
  delete = 'delete',
  edit = 'edit',
  read = 'read',
}

export enum SUBJECTS {
  Auth = 'Auth',
  Todo = 'Todo',
}
