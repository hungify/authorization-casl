export enum Roles {
  Admin = 'admin',
  Editor = 'editor',
  Subscriber = 'subscriber',
  Guest = 'guest',
}

export enum Actions {
  Create = 'create',
  Delete = 'delete',
  Edit = 'edit',
  Read = 'read',
  Manage = 'manage',
}

export enum Subjects {
  Auth = 'auth',
  Todo = 'todo',
  All = 'all',
}
